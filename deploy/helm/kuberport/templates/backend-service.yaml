apiVersion: v1
kind: Service
metadata:
  name: {{ include "kuberport.backend.fullname" . }}
  labels:
    {{- include "kuberport.backend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "kuberport.backend.selectorLabels" . | nindent 4 }}
