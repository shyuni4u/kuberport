# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Secrets
.env
.env.local
.env.*.local

# OCI capacity retry — 실제 OCID 가 들어가는 config.env 는 머신 로컬에만 두고 절대 commit 금지.
# (정상 위치는 ~/oci-capacity-retry/config.env. repo 안에 들어오는 건 사고)
scripts/oci-capacity-retry/config.env

# Brainstorming session scratch (Claude Code superpowers)
.superpowers/

# Per-developer Claude Code settings (shared ones can be committed separately)
.claude/settings.local.json

# Go (once backend/ lands in Plan 1)
backend/bin/
backend/*.test
backend/coverage.out
backend/vendor/

# Node (once frontend/ lands in Plan 1)
frontend/node_modules/
frontend/.next/
frontend/out/

# Docker / runtime
*.pid

# Git worktrees (superpowers convention)
.worktrees/
